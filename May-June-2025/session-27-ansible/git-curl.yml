---
- name: install nginx Server
  hosts: aws1
  become: yes
  tasks:
  - name: Update APT package index
    apt:
      update_cache: yes
  - name: Install git and curl
    apt:
      name: 
        - git
        - curl
      state: present
  - name: Check Git Version
    # Write your code

  - name: Use Curl Command to check Version
    # Version
    # Curl Command to access google.com